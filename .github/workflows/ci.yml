name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  GO_VERSION: "1.22"
  NODE_VERSION: "20"

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
      - name: golangci-lint
        uses: golangci/golangci-lint-action@v6
        with:
          version: latest
      - name: License header check
        run: ./hack/check-license-headers.sh
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      - name: Lint Web UI
        working-directory: web-ui
        run: |
          npm ci
          npm run lint
      - name: Helm lint
        run: helm lint helm/agenttier/

  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        k8s-version: ["1.29", "1.30", "1.31"]
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
      - name: Unit tests
        run: make test
      - name: Upload coverage
        uses: actions/upload-artifact@v7
        with:
          name: coverage-${{ matrix.k8s-version }}
          path: coverage.out

  build:
    runs-on: ubuntu-latest
    needs: [lint, test]
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
      - name: Build binaries
        run: make build
      - name: Build Web UI
        working-directory: web-ui
        run: |
          npm ci
          npm run build
      - name: Build Docker images
        run: |
          docker build -t agenttier-controller:ci -f Dockerfile.controller .
          docker build -t agenttier-router:ci -f Dockerfile.router .
          docker build -t agenttier-webui:ci -f web-ui/Dockerfile web-ui/

  python-sdk:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - name: Install SDK
        working-directory: python-sdk
        run: pip install -e ".[dev]"
      - name: Run tests
        working-directory: python-sdk
        run: pytest tests/ -v
      - name: Type check
        working-directory: python-sdk
        run: mypy src/agenttier/

  # Build the docs site on every PR to catch broken nav entries, missing
  # pages, or dead internal links before they land on main.
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - name: Install MkDocs
        run: pip install mkdocs mkdocs-material
      - name: Build site
        working-directory: docs
        run: mkdocs build --strict --site-dir /tmp/site

  # Security posture checks. These jobs are advisory: a noisy scanner
  # should not permanently block development. Where a finding matters
  # we promote it to the blocking lint/test jobs instead.
  security:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      # Required so we can write results to the repo's Security tab via
      # GitHub's code-scanning SARIF uploader.
      security-events: write
    steps:
      - uses: actions/checkout@v6
        with:
          # gitleaks needs the full history to diff against earlier commits
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      # go.mod integrity: ensure downloaded modules match recorded hashes.
      - name: go mod verify
        run: go mod verify

      # govulncheck scans Go source for CVEs in imported modules
      # (vuln.go.dev). **Advisory** for now: we are pinned to Go 1.22
      # while Dependabot's grouped upgrade PRs (k8s.io/* 0.36, OTel 1.43,
      # prometheus 1.23) are all waiting on a coordinated Go 1.25 bump.
      # The stdlib CVEs govulncheck flags are only fixed in Go 1.25.x,
      # so blocking on them would require the very upgrade we haven't
      # done yet. Promote to blocking as part of the Go-toolchain bump
      # task.
      - name: govulncheck
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          govulncheck ./... || true

      # Static analysis for Go-specific security bugs (unsafe pointers,
      # integer overflow, weak crypto, SQL injection patterns, etc.).
      # Advisory — findings go to the Security tab but don't block the
      # build. Promote to blocking once the baseline is clean.
      - name: gosec
        uses: securego/gosec@master
        with:
          args: "-exclude-dir=test -exclude-generated -fmt=sarif -out=gosec.sarif ./..."
        continue-on-error: true

      - name: Upload gosec SARIF to code-scanning
        if: always()
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: gosec.sarif
          category: gosec
        continue-on-error: true

      # Trivy filesystem scan: picks up vulnerable dependencies in the
      # repo itself (go.sum, package-lock.json, python-sdk pyproject).
      # **Advisory** for now: some CRITICAL findings (e.g. grpc
      # CVE-2026-33186) require a dependency upgrade that pulls Go
      # 1.24+, which we are holding for a coordinated toolchain bump
      # alongside OTel, k8s.io, and prometheus majors. SARIF uploads to
      # the Security tab so we see what is in flight, but the job does
      # not fail until the toolchain moves.
      - name: Trivy filesystem scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: "fs"
          scan-ref: "."
          format: "sarif"
          output: "trivy-fs.sarif"
          severity: "CRITICAL,HIGH"
          ignore-unfixed: true
        continue-on-error: true

      - name: Upload Trivy SARIF to code-scanning
        if: always()
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: trivy-fs.sarif
          category: trivy-fs
        continue-on-error: true

      # Secret scanning: catches anything that looks like a credential
      # that got committed. Complements GitHub's own secret scanning by
      # checking newly-added files in PRs.
      - name: gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Exit 0 even on findings; treat as advisory.
          GITLEAKS_ENABLE_COMMENTS: "false"
        continue-on-error: true

      # Container image scans. These run only on pushes to main so we
      # don't rebuild on every PR (it's slow and the build job already
      # validates the images); the result is still written to the
      # Security tab for baseline visibility.
      - name: Build controller image for scanning
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: docker build -t agenttier-controller:scan -f Dockerfile.controller .

      - name: Trivy image scan (controller)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: "agenttier-controller:scan"
          format: "sarif"
          output: "trivy-controller.sarif"
          severity: "CRITICAL,HIGH"
          ignore-unfixed: true
        continue-on-error: true

      - name: Upload Trivy controller image SARIF
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: trivy-controller.sarif
          category: trivy-controller
        continue-on-error: true
